# Production Readiness Plan - Hades Bug Root Cause Awareness - 2026-07-07

## Executive Summary

Questo piano copre il restante lavoro per arrivare all'ambizione: un agente
locale che puo' spiegare la causa precisa di un bug anche quando, durante la
diagnosi, non ha accesso diretto al filesystem della codebase.

La premessa tecnica va resa esplicita: "senza accesso al source code" non puo'
significare "senza nessuna evidenza di codice mai raccolta". Una root cause
precisa richiede almeno una di queste fonti pregresse e verificabili:

- indice strutturale aggiornato del progetto;
- grafo codice/framework aggiornato;
- evidence pack del bug, con stack trace, log, test falliti, request context e
  versione deploy;
- source slices bounded e policy-compliant per i punti chiamati in causa;
- storia risolta di bug simili e fix gia' verificati.

Stima di prontezza dopo le slice gia' completate: circa 40%.

Il 40% gia' ottenuto include memoria backend condivisa, search live
query-aware, raw chunks esclusi per default, artifact `hades.git_tree.v1` con
`project_index`, timeout live aggressivi differenziati per lookup/status,
source-slice fetch e write, e prefetch memoria reiniettato in ogni chiamata LLM
del tool loop.

Il restante 60% non e' un problema di "piu' memoria". E' un problema di
contratto causale:

1. acquisire evidenza di bug in modo tipizzato;
2. indicizzare davvero codice PHP/Laravel, dipendenze, DB, route, job, eventi e
   punti di ingresso;
3. rendere recuperabili source slices e graph facts con ACL, retention,
   redaction e freshness;
4. insegnare all'agent un workflow di root-cause investigation che blocca claim
   esatti quando evidenza o indice sono stale;
5. provare tutto con test no-codebase, dove il filesystem locale viene
   deliberatamente rimosso.

Il piano e' cross-layer:

- backend: sistema autorevole di memoria, evidence, graph, source slices e
  freshness;
- agent: orchestratore, tool service-gated, policy di confidenza e risposta;
- frontend/desktop/dashboard: intake, stato copertura, review policy e lettura
  evidence;
- operations: test, metriche, runbook e retention.

TencentDB Agent Memory resta escluso: l'architettura attuale non ha bisogno di
un nuovo memory store vendor-specific. Serve invece completare il contratto
Hades gia' avviato.

## Evidence Reviewed

Evidenza locale rivista:

- `plugins/memory/hades_backend/__init__.py`: provider memoria Hades, prefetch
  live/cache, tool `hades_backend_project_memory_search`, domain filters,
  raw chunk opt-in, unmapped-project messaging.
- `agent/turn_context.py`: `on_turn_start()` avviene prima di `prefetch_all()`;
  il prefetch esterno viene calcolato una volta all'inizio del turno.
- `tests/run_agent/test_run_agent.py`: regressione che prova che il contesto
  prefetched viene iniettato in ogni API request del tool loop e non resta nel
  messaggio persistito.
- `agent/system_prompt.py`: il blocco provider memoria esterna entra nel prompt
  volatile, quindi non rompe il prompt statico.
- `hermes_cli/hades_backend_jobs.py`: `sync_git_tree` produce
  `hades.git_tree.v1` con `project_index`; `populate_backend_ast` produce
  `hades.php_graph.v1` per workspace PHP/Laravel, `hades.code_graph.v1` per
  Node/TypeScript/React/Next/Express e mantiene `hades.symbols.v1` per
  workspace Python; `read_source_slice` legge una
  finestra sorgente bounded/redatta per diagnosi line-level approvata.
- `hermes_cli/hades_backend_sync.py`: upload artifact accetta
  `hades.git_tree.v1`, `hades.symbols.v1`, `hades.php_graph.v1` e
  `hades.code_graph.v1`; carica anche source slices approvate; piggyback sync
  usa backoff locale.
- `hermes_cli/hades_backend_client.py`: client espone memory snapshot/search,
  proposals, import bundles, jobs, artifacts, doctor reports e Persephone; non
  espone ancora bug evidence, graph search o source slices.
- `hermes_cli/hades_backend_status.py`: stato locale include jobs, proposals,
  inbox e sync, ma non copertura indice, staleness o diagnosis readiness.
- `apps/desktop/src/app/chat/composer/status-stack/hades-backend-status.ts`:
  desktop mostra solo un summary degraded/warning di backend status.
- `web/src/pages/BackendPage.tsx`: dashboard backend mostra jobs/proposals/inbox;
  non esiste una superficie bug investigation o project coverage.
- `docs/hades/backend.md`, `docs/hades/operations.md`,
  `docs/hades/openapi-hades-v1.json`, `docs/backend-agent-coordination.md`:
  documentano il contratto Hades MVP, memory search e artifacts.
- `docs/production-readiness/2026-07-06-plan.md`: piano precedente di complete
  project awareness generale, da cui questo piano deriva la specializzazione per
  root cause bug.

Evidenza backend remoto rivista via SSH read-only:

- Worktree remoto `/home/ubuntu/dev-sandbox/backend` pulito; ultimi commit:
  `317bb69 feat: index Hades project artifacts in memory search` e
  `98a1f63 feat: add Hades memory search endpoint`.
- `app/Http/Controllers/Hades/MemorySearchController.php`: search live su
  `project_memory_entries`, wiki correnti e `hades_agent_artifacts`; usa LIKE
  e scoring semplice; restituisce `freshness.workspace_head_commit` e
  `index_status=live_query`, ma non blocca claim stale.
- `app/Http/Controllers/Hades/ArtifactController.php`: accetta
  `hades.git_tree.v1`, `hades.symbols.v1`, `hades.php_graph.v1` e
  `hades.code_graph.v1`.
- `app/Services/MemoryImportService.php`: raw chunk import va in
  `raw_chunk_quarantined`, senza creare memory proposal.
- `database/migrations/2026_06_29_000001_create_project_workspace_memory_queue_tables.php`:
  `project_memory_entries` e' generico: `source`, `kind`, `summary`, `payload`.
  Bug evidence, source slices e diagnosis reports sono stati aggiunti come
  modelli tipizzati separati.
- `database/migrations/2026_07_01_000003_create_hades_m4_agent_jobs_tables.php`:
  job Hades tipizzati per capability, payload e result, utili come canale per
  richieste bounded.
- `database/migrations/2026_07_01_000004_create_hades_m5_mvp_tables.php`:
  `hades_agent_artifacts` archivia artifact JSON con schema, hash, truncation e
  redactions.
- `routes/api.php` e `CapabilitiesController.php`: contratto Hades v1 include
  memory, jobs, artifacts, doctor, Persephone, bug reports/evidence, source
  slices e diagnosis reports. Il graph search agent usa artifact search live
  sotto il dominio `artifacts`.
- `tests/Feature/Hades/HadesM3SharedMemoryTest.php`: copertura per memory
  search, wiki, raw chunk omission, artifact domain, bug evidence, awareness,
  source slices, diagnosis reports e import quarantine.
- `tests/Feature/Hades/HadesM5MvpCompletionTest.php`: copertura per artifact
  upload `git_tree` e `symbols`.
- `app/Services/ServerAgentWorkService.php`: backend server-agent ha gia'
  una policy corretta di risposta "solo da contesto fornito", utile come
  principio da riusare per diagnosis.

Comandi principali eseguiti:

```bash
git status --short && pwd && ls
git log --oneline -5
rg --files | sed -n '1,220p'
sed -n '1,240p' pyproject.toml
sed -n '1,220p' package.json
sed -n '1,220p' README.md
sed -n '1,220p' docs/hades/backend.md
sed -n '1,220p' docs/hades/operations.md
ls docs/production-readiness
sed -n '1,220p' docs/production-readiness/2026-07-06-plan.md
rg -n "project_index|hades.project_index|hades.symbols|sync_git_tree|populate_backend_ast|memory_search|source_chunks|freshness|head_commit|workspace_head_commit|stale" hermes_cli plugins agent tests docs -g '*.py' -g '*.md' -g '*.json'
sed -n '320,575p' hermes_cli/hades_backend_jobs.py
sed -n '120,390p' plugins/memory/hades_backend/__init__.py
sed -n '607,690p' plugins/memory/hades_backend/__init__.py
sed -n '437,472p' hermes_cli/hades_backend_sync.py
sed -n '1,180p' apps/desktop/src/app/chat/composer/status-stack/hades-backend-status.ts
sed -n '450,490p' agent/turn_context.py
sed -n '7312,7405p' tests/run_agent/test_run_agent.py
sed -n '437,445p' agent/system_prompt.py
sed -n '100,180p' docs/hades/openapi-hades-v1.json
sed -n '244,320p' docs/hades/openapi-hades-v1.json
sed -n '620,660p' docs/hades/openapi-hades-v1.json
sed -n '248,360p' docs/backend-agent-coordination.md
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && git status --short && git log --oneline -5'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && find app/Http/Controllers/Hades app/Services tests/Feature/Hades database/migrations -type f | sort | sed -n "1,240p"'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "1,260p" app/Http/Controllers/Hades/MemorySearchController.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "1,260p" app/Http/Controllers/Hades/ArtifactController.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "1,260p" app/Services/MemoryImportService.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && grep -RIn "MemorySearch\\|memory/search\\|ArtifactController\\|hades_agent_artifacts\\|project_memory_entries\\|raw_chunk\\|workspace_head_commit\\|index_status\\|bug\\|evidence\\|root cause" app tests database routes --include="*.php" | sed -n "1,260p"'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "260,380p" app/Http/Controllers/Hades/MemorySearchController.php && sed -n "150,215p" app/Services/ServerAgentWorkService.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "1,90p" database/migrations/2026_07_01_000004_create_hades_m5_mvp_tables.php && sed -n "150,360p" tests/Feature/Hades/HadesM3SharedMemoryTest.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "1,180p" tests/Feature/Hades/HadesM5MvpCompletionTest.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && grep -RIn "api/hades/v1\\|MemorySearchController\\|ArtifactController\\|memory/search\\|artifacts" routes app/Providers app/Http --include="*.php" | sed -n "1,220p"'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "1,150p" database/migrations/2026_07_01_000003_create_hades_m4_agent_jobs_tables.php'
ssh ubuntu@162.19.229.31 'cd /home/ubuntu/dev-sandbox/backend && sed -n "150,210p" routes/api.php && sed -n "1,70p" app/Http/Controllers/Hades/CapabilitiesController.php'
```

Comandi falliti o rumorosi:

- Sul backend remoto `rg` non era installato; ho usato `grep` come fallback.
- Un read remoto ha puntato inizialmente a una migrazione con nome errato
  `2026_07_01_000004_create_hades_m4_agent_jobs_tables.php`; il file corretto
  e' `2026_07_01_000003_create_hades_m4_agent_jobs_tables.php`.
- Una scansione locale troppo ampia ha prodotto output troncato; le conclusioni
  di questo piano usano i read mirati successivi, non quell'output come unica
  fonte.

Comandi intenzionalmente non eseguiti:

- test suite completa locale/remota, build frontend, formatter, migrations,
  seed/reset, deploy, install e generatori. Il task e' analysis-only e il
  vincolo della skill consente solo il piano finale.

Stato worktree osservato prima del piano:

- Modifiche pre-esistenti non legate a questo lavoro:
  `hades_agent.egg-info/PKG-INFO`,
  `hades_agent.egg-info/SOURCES.txt`,
  `hades_agent.egg-info/requires.txt`.

## User Answers And Assumptions

Risposte e vincoli confermati:

- TencentDB Agent Memory e' escluso.
- L'obiettivo e' login da qualunque device mantenendo memoria.
- La memoria progetto deve essere backend-owned e condivisa tra sessioni/device.
- L'agent deve essere project-aware in modo evidence-backed, non approssimato.
- Il caso target piu' difficile e' spiegare la causa precisa di un bug senza
  accesso live al source code.

Fatti tecnici confermati:

- Il prefetch memoria esterna viene calcolato a inizio turno e riusato in ogni
  API request del tool loop.
- Hades sa cercare memoria/wiki/artifacts via backend live search; il graph
  search agent usa artifact search live sotto il dominio `artifacts`.
- L'indice progetto attuale ha metadata-first tree/index, una prima graph
  extraction PHP/Laravel, graph search agent su artifact live e source slices
  bounded/redatte; non contiene ancora AST PHP/Laravel completo.
- `populate_backend_ast` produce `hades.php_graph.v1` per PHP/Laravel,
  `hades.code_graph.v1` per Node/TypeScript/React/Next/Express e
  `hades.symbols.v1` per Python.
- Il backend ha bug evidence, awareness/freshness status, source slice store e
  diagnosis reports; restano da rafforzare stale-blocking automatico e suite
  no-codebase.

Assunzioni operative:

- "Causa precisa" significa una risposta che cita evidenze verificabili e puo'
  dire "non determinabile" quando mancano dati.
- "Senza accesso al source code" significa senza filesystem locale nel momento
  della diagnosi, non senza avere mai indicizzato/salvato evidenza source-derived.
- Source slices possono essere salvate solo se policy progetto/utente lo
  permette; altrimenti il backend deve sapere chiedere letture bounded locali.
- Il toolset core deve restare stretto: nuove capacita' Hades devono essere
  service-gated provider tools, CLI+skill o API backend, non core tools.
- Backend e local repo possono avanzare in parallelo con branch coordinati.

Decisioni aperte per il proprietario:

- Policy source: metadata-only, selected source slices, full evidence pack, o
  per-project policy?
- Retention: quanti giorni per logs, source slices, stack trace, request
  payload, diagnosis report e resolved bug memory?
- Confidenza: quando un agent puo' dire "causa precisa" invece di "ipotesi
  probabile"?
- Lingue/framework prioritari dopo Laravel/PHP e Python.
- Modello identita': memoria personale cross-device separata dalla memoria
  progetto, o unificazione con ACL forti?

## Roadmap

Sequenza consigliata:

1. P0-1 Definire il contratto no-codebase e di confidenza.
2. P0-2 Aggiungere bug evidence ingestion backend e outbox locale.
3. P0-3 Rendere freshness e coverage un gate hard per claim precisi.
4. P0-4 Costruire il graph PHP/Laravel v1.
5. P0-5 Aggiungere source slice/evidence pack store e retrieval.
6. P0-6 Aggiungere workflow e tools agent per root-cause investigation.
7. P0-7 Creare una evaluation suite no-codebase.
8. P0-8 Bloccare sicurezza, ACL, redaction e retention per evidence.
9. P1-1 Ingerire log runtime, test, CI e deploy.
10. P1-2 Salvare bug risolti come memoria causale verificata.
11. P1-3 Migliorare search strutturata, FTS/vector e graph traversal.
12. P1-4 Esporre UI desktop/dashboard per investigation e coverage.
13. P1-5 Aggiungere observability/SLO di awareness e diagnosis quality.
14. P1-6 Completare multi-device identity e project binding UX.
15. P1-7 Bonificare note/chunk esistenti in facts verificati.
16. P2-1 Guided bug intake.
17. P2-2 Framework expansion.
18. P2-3 Performance/cost controls.
19. P2-4 Documentazione, runbook e support.
20. P2-5 Governance e quality review continua.

## P0 Blocking Foundations

### P0-1 No-Codebase Diagnosis Contract

- Area: Backend, agent, docs.
- Objective: Definire cosa l'agent puo' affermare quando non vede il filesystem
  locale e quali evidenze servono per una root cause precisa.
- Evidence: Il backend remoto ha una policy simile in
  `ServerAgentWorkService.php`: rispondere solo dal contesto fornito e dire cosa
  manca. Il provider locale oggi invita a cercare exact evidence ma non impone
  una scala di confidenza.
- Affected paths:
  - Local: `docs/hades/backend.md`, `docs/hades/operations.md`,
    `docs/hades/openapi-hades-v1.json`, `docs/backend-agent-coordination.md`,
    eventuale nuova skill `skills/hades-bug-diagnosis`.
  - Remote: docs API backend e seed/capability catalog.
- What/How:
  - Introdurre livelli di evidenza: `metadata_only`, `graph_supported`,
    `source_slice_supported`, `runtime_evidence_supported`,
    `verified_root_cause`.
  - Introdurre livelli di risposta: `not_enough_evidence`, `hypothesis`,
    `likely_cause`, `precise_cause`.
  - Definire che `precise_cause` richiede almeno: bug evidence, graph path,
    freshness valida e source slice o fact verified-from-code.
  - Definire formato di citazione: ogni claim causale deve puntare a evidence
    id, artifact id, source slice id, log id o resolved bug id.
  - Definire comportamento stale: niente claim precisi se HEAD o coverage non
    sono compatibili.
- Dependencies: Nessuna dipendenza tecnica, ma serve decisione policy source.
- Acceptance criteria:
  - Documentazione contiene definizione di "no-codebase mode".
  - OpenAPI o docs Hades elencano campi `confidence`, `evidence_refs`,
    `freshness`, `coverage`.
  - Le risposte agent in diagnosis mode hanno disclaimer solo quando serve, non
    come rumore fisso.
- Verification:
  - Test unit su formatter/policy agent per impedire `precise_cause` senza
    evidence refs.
  - Test docs/OpenAPI se presente.
  - Review manuale di 3 esempi: metadata-only, likely cause, precise cause.

### P0-2 Bug Evidence Ingestion

- Area: Backend, local CLI, agent.
- Objective: Salvare bug report e osservazioni runtime in un modello dati
  tipizzato, non come note generiche.
- Evidence: Backend oggi ha `project_memory_entries` generico e `hades_agent_jobs`
  capability-based, ma nessun endpoint o tabella per bug case/evidence.
- Affected paths:
  - Remote: nuove migrations per `hades_bug_reports`,
    `hades_bug_evidence_items`, `hades_bug_diagnoses`; nuovi controller
    `BugReportController`, `BugEvidenceController`, routes
    `/api/hades/v1/bug-reports`, `/api/hades/v1/bug-evidence`.
  - Local: `hermes_cli/hades_backend_client.py`,
    `hermes_cli/hades_backend_cmd.py`, `hermes_cli/hades_backend_db.py`,
    `plugins/memory/hades_backend/__init__.py`.
  - Tests: remote `tests/Feature/Hades`, local `tests/hermes_cli`,
    `tests/agent`.
- What/How:
  - Definire `bug_report`: project, workspace, title, symptom, severity,
    environment, affected route/task/session, observed_at, reporter.
  - Definire `evidence_item`: kind (`stack_trace`, `log_excerpt`,
    `failing_test`, `http_request`, `http_response`, `browser_console`,
    `deploy_version`, `config_snapshot`, `user_steps`, `screenshot_ref`),
    payload bounded, redactions, sha256, source, occurred_at.
  - Aggiungere local outbox per salvare evidence quando backend offline.
  - Aggiungere CLI `hades backend bug submit` o comando equivalente.
  - Consentire all'agent di creare evidence solo con payload redatto e bounded.
  - Collegare evidence a memory/wiki/artifact refs, non duplicare raw content in
    `project_memory_entries`.
- Dependencies: P0-8 redaction/retention minima; schema API backend.
- Acceptance criteria:
  - Un bug case puo' essere creato con stack trace e failing test senza source
    live.
  - Ogni evidence item ha hash, kind, size, redaction count e retention class.
  - Backend rifiuta evidence troppo grande, segreta o senza project binding.
  - Local sync ritenta upload outbox in modo idempotente.
- Verification:
  - Remote feature tests su create/list/search bug evidence.
  - Local client tests con HTTP mock.
  - Redaction tests per token/API key nei payload.
  - E2E con backend non raggiungibile -> outbox -> sync riuscito.

### P0-3 Freshness And Coverage Gate

- Area: Backend, local sync, agent.
- Objective: Impedire root cause precise quando indice, source slices o bug
  evidence non sono coerenti col commit/deploy che ha prodotto il bug.
- Status 2026-07-07: completata terza tranche local+backend. Backend espone
  `GET /api/hades/v1/project-awareness/status`; local status/provider e desktop
  status stack mostrano awareness/freshness; artifact upload include HEAD
  metadata. Diagnosis report high/medium sono ora bloccati se freshness non e'
  `current`, se mancano `evidence_refs`, o se l'awareness corrente non e'
  `diagnosable_without_source=true`, sia nel provider locale sia nel backend
  remoto (`74bc64c feat: gate Hades diagnoses on awareness coverage`). Restano
  suite regression piu' ampie e UI guidance dedicata.
- Evidence: `MemorySearchController` restituisce `workspace_head_commit` e
  `index_status=live_query`, ma non confronta artifact indexed commit,
  coverage o stale reason. Local `sync_git_tree` non include ancora un gate
  consumabile dal diagnosis workflow.
- Affected paths:
  - Remote: `MemorySearchController`, artifact storage/search, workspace binding
    model, nuove query coverage.
  - Local: `hermes_cli/hades_backend_jobs.py`,
    `hermes_cli/hades_backend_status.py`, `plugins/memory/hades_backend`.
  - UI: `web/src/pages/BackendPage.tsx`,
    `apps/desktop/src/app/chat/composer/status-stack/hades-backend-status.ts`.
- What/How:
  - Salvare per artifact: `indexed_head_commit`, `indexed_at`,
    `workspace_head_commit`, `dirty_state`, `coverage_summary`.
  - Aggiungere endpoint/status `project-awareness/status` o estendere
    capabilities/status con coverage per domains: routes, graph, source slices,
    bug evidence, tests, logs.
  - Calcolare `freshness_status`: `current`, `stale`, `unknown`,
    `partial`, `policy_blocked`.
  - Il tool agent deve ricevere freshness in ogni search/fetch response.
  - Il workflow diagnosis deve bloccare `precise_cause` se bug deploy commit
    non e' coperto.
- Dependencies: P0-1 contract; P0-4/P0-5 for coverage domains completi.
- Acceptance criteria:
  - Status mostra se il progetto e' diagnosticabile senza filesystem.
  - Search results distinguono "matching ma stale" da "matching current".
  - L'agent non produce claim precisi quando freshness e' stale/unknown.
  - Manual sync aggiorna coverage e rimuove stale warning.
- Verification:
  - Tests unit su stale comparison commit.
  - Feature tests remote su freshness response.
  - Agent tests che simulano stale search e verificano risposta conservative.
  - UI tests per warning coverage.

### P0-4 PHP/Laravel Graph V1

- Area: Local indexer, backend artifacts, search.
- Objective: Passare da project metadata a code graph Laravel utile per causal
  debugging.
- Status 2026-07-07: completata una sesta tranche locale
  `hades.php_graph.v1`. Il graph ora copre route, middleware route-level,
  classi, metodi, relazioni Eloquent, static call con import PHP risolti,
  instantiation edge, model-to-table, migrations/tables/columns/index/foreign
  keys, policies/gates, FormRequest/request validation fields, dispatched jobs,
  emitted events, event listeners, Artisan commands, scheduler command/job
  edges, DB query-table edges, Eloquent query calls e riferimenti
  `config()`/`env()` redatti come chiavi. La quarta tranche aggiunge dependency
  injection da type hints, container bindings, model observers, view/Inertia refs
  e broadcast channel refs. La quinta tranche aggiunge Blade template dependency
  graph metadata-only: view symbols, Blade layout/include edges, component edges
  e Livewire component refs. La sesta tranche aggiunge fallback locale
  metadata-only per `hades_backend_graph_search` e
  `hades_backend_graph_traverse` su artifact graph cached/job result, marcato
  come `freshness.status=cached`. Restano da ampliare parser strutturati/AST
  reale, query builder avanzato e FTS/vector/rerank generalizzati.
- Evidence: prima di questa slice `populate_backend_ast` leggeva solo `.py`;
  `project_index` vedeva route, dependencies e migrations ma non controller
  methods, service calls, model relationships, events, jobs, policies,
  middleware o DB usage.
- Affected paths:
  - Local: `hermes_cli/hades_backend_jobs.py`, possibili nuovi moduli
    `hermes_cli/hades_php_indexer.py`, `hermes_cli/hades_laravel_indexer.py`.
  - Remote: `ArtifactController`, `MemorySearchController`, OpenAPI,
    migration/index per artifact metadata.
  - Tests: `tests/hermes_cli/test_hades_backend_sync_runner.py`, remote
    `tests/Feature/Hades`.
- What/How:
  - Aggiungere schema `hades.php_graph.v1` o `hades.code_graph.v1`.
  - Estrarre:
    - routes -> controller/action/middleware/name/file/line;
    - controllers -> methods, request validation, service calls;
    - services -> public methods, injected deps, called repos/clients;
    - models -> table, fillable/casts/relations/scopes;
    - migrations -> tables, columns, indexes, foreign keys;
    - jobs/events/listeners/commands/schedulers;
    - policies/gates/middleware;
    - config keys read through `config()` and env references as redacted refs.
  - Preferire parser strutturato dove possibile; fallback regex solo per
    Laravel conventions semplici.
  - Non salvare raw source nel graph; salvare path, line range, symbol hash e
    short signature.
  - Accettare nuovo schema artifact lato backend e renderlo searchable.
- Dependencies: P0-8 policy; P0-3 freshness fields.
- Acceptance criteria:
  - Un route/controller/service bug puo' essere collegato a symbols e call edges.
  - Artifact include coverage e omission reasons.
  - Backend memory search o graph search trova symbol, route e edge.
  - Il graph non include secret/raw file content.
- Verification:
  - Fixture Laravel mini-app con route, controller, service, model, migration.
  - Local tests su graph extraction deterministica.
  - Remote feature tests su upload/search `hades.php_graph.v1`.
  - Golden schema validation con truncation/omission.

### P0-5 Source Slice And Evidence Pack Store

- Area: Backend, local jobs, agent retrieval.
- Objective: Consentire all'agent di leggere evidence source-derived bounded
  senza accesso al filesystem live e senza importare raw chunks nella memoria
  ordinaria.
- Status 2026-07-07: completata una seconda tranche source/evidence path:
  tabella backend `hades_source_slices`, endpoint `/source-slices`, coverage
  awareness, job locale `read_source_slice`, upload da sync/approve-job e tool
  provider `hades_backend_source_slice_fetch`; aggiunti inoltre store/search
  backend `/evidence-packs`, coverage `evidence_packs`, client locale e tool
  provider `hades_backend_evidence_pack_search` /
  `hades_backend_evidence_pack_create`.
- Evidence: `read_files` puo' inviare contenuto bounded dopo conferma, ma il
  risultato non e' un source slice store interrogabile; raw chunks sono
  quarantined e non diventano facts.
- Affected paths:
  - Remote: nuove tabelle `hades_source_slices`,
    `hades_evidence_packs`; controller `/source-slices`, `/evidence-packs`.
  - Local: `hades_backend_jobs.py` nuove capability `sync_source_slices`,
    `read_source_slice`, client methods.
  - Provider: nuovi tool service-gated `hades_backend_source_slice_fetch`,
    `hades_backend_evidence_pack_search`,
    `hades_backend_evidence_pack_create`.
- What/How:
  - Definire source slice: project, workspace, commit, path, start_line,
    end_line, language, symbol, sha256, content_redacted, retention_class,
    policy, created_from_job.
  - Source slices devono essere create solo da:
    - job approvato;
    - policy progetto che consente selected source;
    - automatic safe slice per traceback frame, se permesso.
  - Evidence pack aggrega: bug report, graph refs, source slices, logs, tests,
    deploy metadata, previous similar bugs.
  - Tool fetch deve restituire contenuto bounded e citabile, non raw project dump.
  - Automatic memory prefetch continua a escludere source content.
- Dependencies: P0-2 bug evidence; P0-8 security; P0-3 freshness.
- Acceptance criteria:
  - Da stack trace file:line l'agent puo' recuperare solo le linee rilevanti.
  - Un evidence pack e' scaricabile senza filesystem e contiene refs
    verificabili.
  - Raw source non entra in `project_memory_entries` come memory note.
  - Policy denial produce richiesta di bounded local read, non hallucination.
- Verification:
  - Tests remote su create/fetch source slice con ACL e size limits.
  - Tests local su `read_source_slice` job.
  - Agent tests su source slice fetch prima di claim causale.
  - Security tests su redaction e path traversal.

### P0-6 Agent Root-Cause Investigation Workflow

- Area: Agent, provider tools, skills.
- Objective: Dare all'agent una procedura obbligata per diagnosi bug evidence-
  backed, con tool dedicati e output causale citabile.
- Status 2026-07-07: completata la prima slice agent/backend con tool
  `hades_backend_bug_evidence_search`, `hades_backend_graph_search`,
  `hades_backend_source_slice_fetch`, `hades_backend_diagnosis_report_create` e
  skill `hades-bug-diagnosis` per imporre ordine awareness -> evidence ->
  graph -> source slices -> persisted diagnosis report. La suite locale
  no-codebase misura ora anche `tool_order_coverage`, quindi una diagnosi che
  usa gli strumenti giusti ma salta l'ordine obbligato fallisce il quality gate.
  Inoltre `tests/run_agent/test_run_agent.py` copre ora un loop
  `run_conversation` reale con modello finto: i soli tool esposti sono quelli
  Hades no-codebase, la sequenza awareness -> evidence -> graph -> source
  slice -> report viene eseguita prima della risposta finale, e i tool
  filesystem restano esclusi. Restano da implementare valutazioni end-to-end
  con provider/modello reale.
- Evidence: Il provider Hades espone ora il percorso diagnosis live-only; i
  report sono persistiti dal backend in `hades_diagnosis_reports`.
- Affected paths:
  - Local: `plugins/memory/hades_backend/__init__.py`,
    `agent/memory_manager.py`, nuova skill `skills/hades-bug-diagnosis`,
    tests agent.
  - Backend client: `hermes_cli/hades_backend_client.py`.
- What/How:
  - Aggiungere tool service-gated:
    - `hades_backend_bug_evidence_search`;
    - `hades_backend_graph_search`;
    - `hades_backend_source_slice_fetch`;
    - `hades_backend_diagnosis_report_create`.
  - Workflow agent:
    1. identificare bug report/evidence;
    2. leggere freshness/coverage;
    3. cercare route/symbol/graph path;
    4. recuperare source slices minime;
    5. confrontare con logs/tests;
    6. produrre cause, mechanism, affected symbols, fix direction, missing
       evidence, confidence;
    7. salvare diagnosis report se richiesto o se workflow completato.
  - Il prompt/tool description deve vietare claim precisi senza evidence refs.
  - Timeout agent Hades differenziati: lookup/status/traversal/search 1s,
    source-slice fetch 1.5s, write/create/promote 2s.
- Dependencies: P0-1/P0-5.
- Acceptance criteria:
  - In no-codebase mode l'agent usa i tool Hades prima di rispondere.
  - Output contiene `root_cause`, `mechanism`, `evidence_refs`, `confidence`,
    `freshness`, `next_verification`.
  - Se manca evidence, dice esattamente cosa manca.
  - Tool fallback cache non viene spacciato per live/current.
- Verification:
  - Agent tests con fake backend: evidence completa -> precise cause.
  - Agent tests: stale graph -> no precise cause.
  - Agent tests: metadata-only -> hypothesis.
  - Prompt/tool schema tests per output bounded.

### P0-7 No-Codebase Evaluation Suite

- Area: QA, backend, agent.
- Objective: Dimostrare che la diagnosi funziona quando il filesystem sorgente
  non e' disponibile.
- Status 2026-07-07: avviata suite locale no-codebase con evaluator
  `hermes_cli/hades_no_codebase_eval.py`, fixture
  `tests/fixtures/hades/no_codebase_bug_cases.json` e test
  `tests/agent/test_hades_bug_diagnosis_no_codebase.py`. La suite copre 5 casi
  completi, 2 insufficient-evidence, report accuracy/coverage, blocco tool di
  accesso raw source e freshness coverage per impedire high/medium su evidence
  stale. `hades backend quality-report --no-codebase-eval ...` propaga ora
  `repair_freshness_coverage`, `repair_awareness_coverage` e
  `repair_hades_tool_order` come blocker, e `.github/workflows/tests.yml`
  esegue un job dedicato `Hades no-codebase quality gate`. Il backend remoto ha
  ora anche `HadesNoCodebaseDiagnosisTest`,
  che prova un percorso API
  source-free completo: graph artifact current, stack-trace evidence, source
  slice bounded, evidence pack, awareness diagnosable, graph traversal, source
  retrieval e diagnosis report high/final. Il follow-up remoto `74bc64c feat:
  gate Hades diagnoses on awareness coverage` aggiunge il caso negativo
  source-free incompleto: graph current e bug evidence non bastano senza source
  slice coverage, `diagnosable_without_source=false`, high/final viene
  rifiutato e un report `insufficient` resta salvabile. Il follow-up remoto
  `aead8bc test: cover stale no-codebase diagnosis` copre anche graph/source
  slices stale: awareness diventa `stale`, high/final viene rifiutato e
  `insufficient` resta disponibile per registrare il blocco senza claim preciso.
- Evidence: I test provider/sync/artifacts non misuravano diagnosi root cause
  end-to-end senza source; ora esiste un primo gate locale strutturato.
- Affected paths:
  - Local tests: nuovo `tests/agent/test_hades_bug_diagnosis_no_codebase.py`,
    `tests/hermes_cli/test_hades_backend_bug_evidence.py`.
  - Remote tests: `tests/Feature/Hades/HadesBugEvidenceTest.php`,
    `HadesGraphSearchTest.php`.
  - Fixtures: mini progetti Laravel/PHP e Python con bug noti.
- What/How:
  - Creare fixture bug cases:
    - route 500 per service dependency nullo;
    - migration/schema mismatch;
    - auth middleware/policy mismatch;
    - queue job serialization failure;
    - config/env missing redacted;
    - regression gia' risolta.
  - Precaricare backend con graph, source slices, logs, tests e deploy commit.
  - Eseguire agent in cwd non legata alla codebase o con tools file disabilitati.
  - Valutare risposta con match strutturato: root cause id, evidence refs,
    confidence corretta, missing evidence se incompleta, e awareness source-free
    diagnosable per claim high/medium.
- Dependencies: P0-2/P0-4/P0-5/P0-6.
- Acceptance criteria:
  - Almeno 5 bug fixture passano senza accesso filesystem.
  - Almeno 2 casi incompleti vengono classificati come insufficient evidence.
  - Nessun test richiede raw repo access durante diagnosis.
  - Report finale produce coverage e accuracy.
- Verification:
  - Pytest locale con fixture no-codebase e no filesystem.
  - Feature tests remoti con SQLite in-memory. (tranche completate con
    `62691ce test: cover Hades no-codebase diagnosis path` e `74bc64c feat:
    gate Hades diagnoses on awareness coverage`; stale coverage con `aead8bc
    test: cover stale no-codebase diagnosis`)
  - CI gate dedicato lightweight.

### P0-8 Security, ACL, Redaction And Retention Baseline

- Area: Security, backend, local agent, docs.
- Objective: Rendere sicuro salvare logs, stack trace e source slices prima di
  usarli per diagnosis.
- Status 2026-07-07: baseline backend/CLI P0-8 estesa. Il commit remoto
  `a110d52 feat: enforce Hades evidence safety policy` rifiuta bug evidence,
  source slices e diagnosis reports con segreti non redatti o payload oltre
  limite; il commit remoto `d383c4f feat: add Hades privacy retention controls`
  aggiunge export, delete e retention cleanup workspace-scoped con
  dry-run/confirm; il follow-up `6559223 feat: audit Hades privacy operations`
  scrive audit events senza payload raw. Il client locale espone
  `privacy-export`, `privacy-delete` e `retention-cleanup`. Restano dashboard
  policy UI e governance piu' fine.
- Evidence: Esistono redaction helpers e retention classes locali; ora esiste
  anche una policy backend condivisa per content-bearing diagnosis data.
- Affected paths:
  - Remote: migrations evidence/source, policies/middleware, controller
    validation, dashboard roles.
  - Local: `hades_backend_client.py`, `hades_backend_jobs.py`, redaction tests.
  - Docs: `docs/hades/operations.md`, `docs/hades/support-runbook.md`.
- What/How:
  - Classificare retention: `source_metadata`, `source_symbols`,
    `source_slice`, `runtime_log`, `request_payload`, `diagnosis_report`,
    `resolved_bug_memory`.
  - Definire ACL per project/team/user/device/agent.
  - Redigere token, headers, env, cookies, PII, credentials e path sensibili.
  - Aggiungere max size per evidence item e source slice.
  - Aggiungere delete/export per progetto e utente. (prima tranche completata
    workspace-scoped con dry-run/confirm)
  - Loggare audit events senza payload raw. (completato per
    export/delete/retention workspace-scoped)
- Dependencies: Decisione policy source.
- Acceptance criteria:
  - Backend rifiuta payload non redatti o oltre limite. (prima baseline
    completata per bug evidence, source slices e diagnosis reports)
  - Ogni evidence item ha retention e ACL.
  - Dashboard mostra policy prima di abilitare source slices.
  - Doctor/support runbook indica cosa condividere e cosa non condividere.
- Verification:
  - Security tests su secret redaction.
  - Feature tests su unauthorized cross-project access.
  - Retention cleanup tests. (prima tranche remota completata)
  - Audit payload tests che escludono raw source/evidence content. (completato)
  - Manual review di sample log/source slice.

## P1 Production Hardening

### P1-1 Runtime Logs, Test, CI And Deploy Ingestion

- Area: Backend, local CLI, integrations.
- Objective: Raccogliere automaticamente i segnali che rendono causale la
  diagnosi: test falliti, logs runtime, deploy version, request context.
- Status 2026-07-07: avviata prima tranche locale con
  `hades backend ingest-test` e `hades backend ingest-log`. I comandi leggono
  excerpt bounded, redigono segreti, estraggono stack frame leggeri e caricano
  `failing_test`/`log_excerpt` via `create_bug_evidence`.
- Evidence: Hades aveva doctor reports e jobs, ma non ingestion specifica di
  test/log per bug case.
- Affected paths:
  - Local: nuovi comandi `hades backend ingest-test`,
    `hades backend ingest-log`.
  - Remote: endpoints evidence, dashboard upload, CI webhook opzionale.
  - Docs: runbook CI/log.
- What/How:
  - Parser per PHPUnit/Pest, pytest, vitest/jest, Laravel logs, HTTP 500 traces.
  - Collegare evidence a commit/deploy/env.
  - Consentire upload manuale o automatico dopo test failure.
  - Normalizzare stack frames verso graph symbols/source slices.
- Dependencies: P0-2/P0-4/P0-5.
- Acceptance criteria:
  - Un failing test genera evidence item strutturato. (prima tranche locale
    completata)
  - Un Laravel stack trace viene mappato a symbol/line refs. (frame file:line
    estratti localmente)
  - Deploy commit mismatch viene evidenziato.
- Verification:
  - Parser unit tests.
  - E2E local command -> backend evidence.
  - Fixture logs redatti.

### P1-2 Resolved Bug Memory Lifecycle

- Area: Backend memory, agent, dashboard.
- Objective: Trasformare bug risolti in conoscenza duratura e verificata senza
  inquinare memory generica.
- Status 2026-07-07: completata prima tranche backend+agent. I diagnosis
  report finali con confidence `high`/`medium` possono essere promossi a
  `project_memory_entries.kind=resolved_bug` via endpoint Hades e tool
  `hades_backend_resolved_bug_promote`. Search backend/local espone
  `kind=resolved_bug`, boost ranking e flag `stale` quando l'HEAD workspace
  cambia rispetto al commit della diagnosi.
- Evidence: Memory proposals esistevano, ma non c'era schema per
  "symptom -> cause -> fix -> regression test".
- Affected paths:
  - Remote: `project_memory_entries` kind `resolved_bug` o nuova tabella
    dedicata, proposal review.
  - Local provider: memory search domains e ranking.
  - UI: dashboard bug case detail.
- What/How:
  - Schema resolved bug: symptom, root cause, mechanism, affected symbols,
    fix commit/PR, regression test, evidence refs, confidence, validity window.
    (prima tranche completata)
  - Salvare solo dopo conferma utente o test pass. (enforced da
    `verification_status`)
  - Renderlo prioritario per bug simili, ma separato da source chunks. (search
    boost + `kind=resolved_bug`)
- Dependencies: P0-2/P0-6/P0-7.
- Acceptance criteria:
  - Diagnosis report puo' essere promosso a resolved bug memory. (completato)
  - Search trova bug simili per symptom/stack/symbol. (completato per symptom,
    root cause, payload, affected symbols)
  - Resolved bug stale se affected symbol cambia. (prima tranche: stale su
    workspace HEAD mismatch; symbol-level invalidation resta P1-3/P1-4)
- Verification:
  - Feature tests promotion/review.
  - Agent tests similar bug recall.
  - Stale invalidation tests.

### P1-3 Structured Search, FTS, Vector And Graph Traversal

- Area: Backend search, storage, agent tools.
- Objective: Superare LIKE search per recall piu' affidabile e meno rumoroso.
- Status 2026-07-07: completata quinta tranche backend. Nuovo endpoint
  backend `GET /api/hades/v1/graph/traverse` e tool locale
  `hades_backend_graph_traverse` attraversano il grafo corrente da route,
  symbol, file, class o method con depth/limit bounded, freshness e provenance.
  Il tool `hades_backend_project_memory_search` supporta inoltre filtri
  strutturati `kind`, `schema`, `source`, `symbol` e `path`; il backend remoto
  applica gli stessi filtri a memory/wiki/artifacts e restituisce `match_fields`.
  Il fallback locale di `hades_backend_graph_search` usa ora ranking BM25
  leggero su nodi/edge del graph cache, con tokenizzazione camel/snake/FQCN. La
  quinta tranche migliora il ranking backend memory/artifact/wiki con scoring
  multi-token density-aware: match concisi e completi battono note rumorose piu'
  recenti con gli stessi token.
- Evidence: `MemorySearchController` filtra ancora con LIKE su
  summary/payload/artifact JSON, ma il ranking non e' piu' un semplice conteggio
  token uguale per documenti brevi e lunghi. Resta da introdurre FTS/vector vero
  per dataset grandi.
- Affected paths:
  - Remote: search services, DB indexes, optional vector/FTS tables.
  - Local: provider result parsing e ranking.
- What/How:
  - Separare endpoint: memory search, bug evidence search, graph search, source
    slice search. (graph traversal dedicato completato)
  - Usare FTS per text evidence e structured filters per project/commit/symbol.
    (terza tranche: structured filters backend/local per kind/schema/source/
    symbol/path; quarta tranche: BM25 locale per graph cache; quinta tranche:
    ranking backend density-aware; resta FTS/vector backend per dataset grandi)
  - Graph traversal: route -> controller -> service -> model -> migration/log.
    (prima tranche route/controller/service completata su `hades.php_graph.v1`)
  - Vector search solo come complemento, mai come unica fonte per claim precisi.
- Dependencies: P0 graph/evidence schemas.
- Acceptance criteria:
  - Query stack trace/symbol trova evidence corretta con meno falsi positivi.
    (terza tranche completata per filtri strutturati symbol/path/schema/source;
    quarta tranche migliora graph cache fallback con BM25; quinta tranche
    migliora text ranking backend per query ambigue; resta FTS/vector backend)
  - Results hanno score, match fields, freshness e provenance. (completato per
    graph traversal e memory search strutturata)
  - Agent puo' chiedere traversal bounded. (completato)
- Verification:
  - Ranking tests con fixture ambigue. (quinta tranche coperta da feature test
    backend: match conciso multi-token vince su nota rumorosa piu' recente)
  - Performance tests su dataset medio.
  - Regression tests che LIKE-only avrebbe fallito.

### P1-4 Dashboard And Desktop Investigation Surfaces

- Status: terza tranche completata. La dashboard backend mostra awareness
  health, coverage per binding, diagnosis quality/readiness, confidence signals,
  missing-evidence blockers e il pannello `Identity recovery` con project memory
  portability, workspace mapping corrente, source-free readiness e next action;
  il desktop status stack segnala awareness partial/degraded/unmapped.
- Area: Frontend, desktop, dashboard.
- Objective: Rendere visibile e governabile lo stato di project awareness e bug
  diagnosis.
- Evidence: Dashboard backend page oggi mostra jobs/proposals/inbox; desktop
  mostra solo status warning generico.
- Affected paths:
  - Web: `web/src/pages/BackendPage.tsx`, nuove pagine bug cases/coverage.
  - Desktop: status stack, right rail/artifacts, settings memory provider.
  - Backend: dashboard APIs per coverage, bug reports, evidence packs.
- What/How:
  - Dashboard project coverage: memory, graph, source slices, evidence, stale.
  - Bug case detail: evidence timeline, graph path, source slices, diagnosis,
    confidence.
  - Controls: approve source-slice policy, request reindex, review diagnosis
    promotion.
  - Desktop: compact warning quando progetto non e' diagnosticabile o stale.
- Dependencies: P0-2/P0-3/P0-5.
- Acceptance criteria:
  - Utente vede perche' l'agent puo' o non puo' diagnosticare senza codebase.
    Prima tranche completata per awareness locale; seconda tranche completata
    per next action dashboard multi-device/workspace; terza tranche completata
    per diagnosis quality/readiness dashboard.
  - Review di source/evidence policy avviene in UI, non solo CLI.
    Ancora da fare per policy controls completi.
  - Desktop non nasconde stale state dietro status generico.
    Prima tranche completata per awareness status.
- Verification:
  - UI unit tests su summary/coverage states. Prima tranche completata per
    desktop status summary; dashboard coperta da typecheck.
  - Playwright/screenshot smoke se si implementano nuove pagine.
  - API contract tests.

### P1-5 Observability And Diagnosis Quality SLOs

- Status: terza tranche locale/dashboard completata. `hades backend status --json`
  espone `awareness` top-level e per binding con segnali locali di memory cache,
  artifact index, source slices, bug evidence, readiness source-free e missing
  evidence. La no-codebase quality gate misura anche `awareness_coverage` e
  genera il blocker `repair_awareness_coverage` quando un run preciso non prova
  `diagnosable_without_source=true`; misura inoltre `tool_order_coverage` e
  genera `repair_hades_tool_order` quando i tool Hades sono presenti ma usati
  fuori sequenza. La dashboard backend espone readiness, blocker, confidence
  distribution e ultimo segnale di qualita' derivati dallo stesso payload.
- Area: Operations, logging, metrics.
- Objective: Misurare copertura e qualita' della diagnosi, non solo successo
  tecnico di sync.
- Evidence: Hades gia' logga `sync.*`, `artifact.uploaded`, doctor reports; non
  ci sono metriche su coverage/diagnosis correctness.
- Affected paths:
  - Local logging/status, remote audit/metrics, docs release gates.
- What/How:
  - Metriche: graph coverage %, stale age, source slice coverage, evidence item
    counts, diagnosis confidence distribution, unresolved missing-evidence rate.
  - Structured logs per tool diagnosis: search counts, stale blocks, evidence
    refs used, no raw payload.
  - Release gate: no-codebase diagnosis suite accuracy.
- Dependencies: P0-7.
- Acceptance criteria:
  - `hades backend status --json` o doctor espone awareness health. Prima
    tranche completata lato `status --json`.
  - Dashboard mostra trend e ultimi failure. Prima tranche completata per latest
    quality signal e blocker correnti; trend storici e failure drill-down restano
    da fare.
  - Diagnosi non verificabili sono conteggiate come incomplete, non success.
    Prima tranche completata nel payload locale: `diagnosable_without_source`
    resta false quando manca bug evidence o coverage locale.
- Verification:
  - Unit tests status payload. Prima tranche completata.
  - Log redaction tests.
  - Release gate docs update.

### P1-6 Multi-Device Identity And Project Binding UX

- Status: seconda tranche locale completata. `hades backend status --json`
  espone `identity` separando memoria locale di profilo, project memory
  portabile e workspace binding locale/current; `identity.login_recovery`
  comunica se questo device puo' usare memoria progetto senza il vecchio
  filesystem, se il workspace corrente e' mappato, e il prossimo comando utile
  per link/sync/evidence.
- Area: Backend auth, local setup, frontend.
- Objective: Rendere cross-device la memoria senza confondere memoria personale,
  project memory e workspace binding.
- Evidence: Esistono bootstrap token, agent register, workspace binding e
  derived agent token; serve UX/policy per usarli da qualunque device in modo
  comprensibile.
- Affected paths:
  - Remote: Hades auth/capabilities/dashboard admin.
  - Local: `hades backend bootstrap`, `hades project link`, status/doctor.
  - UI: onboarding desktop/dashboard.
- What/How:
  - Separare domini:
    - personal memory: utente/device/account;
    - project shared memory: progetto/team;
    - workspace binding: repo locale specifica e freshness.
  - Onboarding: "questo device e' collegato a project X, commit Y, policy Z".
  - Recovery: login nuovo device -> scarica memoria condivisa -> richiede
    indexing se manca workspace/source.
- Dependencies: P0-3/P0-8.
- Acceptance criteria:
  - Nuovo device puo' richiamare memoria progetto senza accedere al vecchio
    filesystem.
    Prima tranche completata lato status: project memory e' marcata portabile e
    distinta dal workspace locale; seconda tranche aggiunge next action
    `login_recovery` per nuovo device/workspace corrente. Recovery/onboarding
    remoto resta.
  - Nuovo device non puo' leggere source slices se ACL/policy non lo consente.
    Gia' coperto dalla policy source-slice backend; UI recovery resta.
  - Status distingue personal memory da project memory.
    Prima tranche completata.
- Verification:
  - Multi-agent/multi-device feature tests. Ancora da estendere lato remoto.
  - Local bootstrap/link tests. Prima tranche status completata.
  - UI onboarding tests.

### P1-7 Notes And Raw Chunk Backfill

- Status: quinta tranche backend completata. `hades backend backfill-note`
  classifica raw chunks, estrae candidate facts route-handler raggruppati,
  mantiene l'output review-only/non automatic recall e puo' creare proposal
  pending solo con opt-in `--create-proposals`. I candidate facts hanno
  fingerprint stabile e il comando salta proposals duplicate sul secondo run.
  La terza tranche aggiunge diagnostica `quality_score`, `quality_grade`,
  `quality_issues` e `promotion_state` per distinguere quarantena, candidate
  facts reviewable e note che richiedono strutturazione manuale. La quarta
  tranche impedisce al backend di auto-promuovere `note_backfill_candidate` in
  `project_memory_entries`: restano `pending` per review manuale e il client
  locale marca la proposal come `submitted` per non rispedirla a ogni sync. La
  quinta tranche consente alla review dashboard di accettare una proposal
  backfill e creare una sola memoria `verified_note_fact` con provenance del
  candidate fact; il payload promosso include freshness del workspace binding
  (`workspace_head_commit`, `index_status=reviewed_note_fact`).
- Area: Data quality, backend import, agent memory.
- Objective: Bonificare note esistenti e chunk grezzi in facts/wiki/evidence
  verificati.
- Evidence: L'esempio dell'utente mostra chunk `hades.backend_wiki.file_chunk.v1`
  molto grezzo. Ora viene escluso/quarantined, ma va trasformato se utile.
- Affected paths:
  - Remote: memory import service, wiki refresh, review queues.
  - Local: sync/import commands and docs.
- What/How:
  - Classificare entries: raw chunk, extracted fact, verified wiki,
    unresolved note, stale note.
  - Pipeline review: raw chunk -> candidate facts -> wiki/evidence refs ->
    accepted memory.
  - De-duplicare route/link ripetitivi in structured artifacts.
    Prima tranche raggruppa route per handler; seconda tranche deduplica
    proposal locali via fingerprint candidate fact; terza tranche aggiunge
    segnali qualità per review; quarta tranche aggiunge review gate backend
    per evitare promozione automatica dei raw-chunk backfill; quinta tranche
    aggiunge promozione manuale idempotente a project memory verificata.
  - Evitare che note rumorose dominino recall automatico.
- Dependencies: P0-1/P0-8.
- Acceptance criteria:
  - Nessun raw chunk diventa memoria automatica.
    Prima tranche completata lato local preview; quarta tranche completata lato
    backend proposal gate.
  - Facts accettati hanno evidence refs e freshness.
    Prima tranche produce candidate facts/proposals con evidence refs;
    accettazione backend resta manuale per `note_backfill_candidate`;
    quinta tranche crea `verified_note_fact` con provenance dopo accettazione
    dashboard e freshness del workspace binding.
  - Recall automatico resta compatto e utile.
    Gia' coperto dal provider; backfill preview raggruppa route ripetute.
- Verification:
  - Import/backfill tests. Prima tranche locale completata.
  - Sample conversion del chunk fornito dall'utente.
    Prima tranche completata con fixture route-handler.
  - Search ranking tests prima/dopo backfill.

## P2 Production Polish

### P2-1 Guided Bug Intake

- Area: UX, agent skill, dashboard.
- Objective: Rendere facile creare bug reports completi senza chiedere
  all'utente di conoscere il modello dati.
- Evidence: P0 richiede evidence strutturata; senza wizard l'input sara'
  incompleto.
- Affected paths: desktop composer, dashboard bug page, optional skill/command.
- What/How: form/wizard per symptom, steps, expected/actual, logs, stack trace,
  failing tests, environment, severity, privacy preview.
- Dependencies: P0-2/P1-4.
- Status: prima tranche CLI completata. `hades backend bug-intake` crea bug
  report strutturati e allega test/log redatti.
- Acceptance criteria: intake crea bug report completo e redatto. Prima tranche
  completata lato CLI; wizard desktop/dashboard resta.
- Verification: UI tests e fixture manuale. Prima tranche coperta da CLI unit
  test redaction/report/evidence.

### P2-2 Framework Expansion

- Area: Indexers, graph, tests.
- Objective: Estendere complete awareness oltre Laravel/PHP.
- Status 2026-07-07: nona tranche locale completata per
  Node/TypeScript/React/Next/Express tramite `hades.code_graph.v1`. Il job
  `populate_backend_ast` ora rileva framework, route Next, page entrypoint,
  route Express, simboli component/function/class/export, import edge e
  dependency manifests senza includere raw source; il backend remoto accetta e
  cerca questi artifact. La seconda tranche aggiunge route/handler metadata per
  FastAPI e Django dentro lo stesso schema `hades.code_graph.v1`, mantenendo
  `hades.symbols.v1` per progetti Python generici senza route web. La terza
  tranche aggiunge route/controller metadata Symfony attribute/annotation dentro
  `hades.php_graph.v1`. La quarta tranche aggiunge schema DB Django models
  source-free dentro `database.tables` con column e foreign key metadata. La
  quinta tranche aggiunge schema DB SQLAlchemy declarative source-free. La
  sesta tranche aggiunge schema DB Prisma source-free da `schema.prisma`. La
  settima tranche aggiunge schema SQL raw source-free per `CREATE TABLE`.
  L'ottava tranche aggiunge schema DB Drizzle source-free da `pgTable`,
  `mysqlTable` e `sqliteTable`. La nona tranche aggiunge schema DB Doctrine
  attribute source-free.
- Evidence: Python symbols MVP esiste; Laravel e' prioritario per Carnovali e
  backend, ma l'agent e' general purpose. Prima di questa tranche i workspace
  JS/TS cadevano nel fallback Python-like o non producevano graph utile.
- Affected paths: `hermes_cli/hades_backend_jobs.py`,
  `hermes_cli/hades_backend_sync.py`, OpenAPI/docs, fixture suites.
- What/How: aggiungere Symfony, Node/TS, React, Python/FastAPI/Django, database
  schema adapters. Prima tranche Node/TS/React completata; seconda tranche
  FastAPI/Django route metadata completata; terza tranche Symfony
  attribute/annotation route metadata completata; quarta tranche Django models
  schema metadata completata; quinta tranche SQLAlchemy declarative schema
  metadata completata; sesta tranche Prisma schema metadata completata; restano
  parser strutturati piu' profondi.
- Dependencies: P0 graph abstractions.
- Acceptance criteria: ogni framework ha route/entrypoint/symbol/test/log map.
  Prima tranche copre route/entrypoint/symbol/import map per Next/React/Express;
  seconda tranche copre route/handler/symbol map per FastAPI/Django; terza
  tranche copre route/controller/symbol map per Symfony; quarta tranche copre
  table/column/foreign-key map per Django models; quinta tranche copre
  table/column/foreign-key map per SQLAlchemy declarative; sesta tranche copre
  table/column/foreign-key map per Prisma; settima tranche copre
  table/column/foreign-key map per SQL raw; ottava tranche copre
  table/column/foreign-key map per Drizzle; nona tranche copre
  table/column/foreign-key map per Doctrine attributes.
- Verification: fixture per framework. Prima tranche coperta da unit test locali
  su graph extraction/upload artifact `hades.code_graph.v1` e feature test
  remoti su upload/search; seconda e terza tranche coperte da unit test locali
  FastAPI/Django/Symfony e regressione Laravel; quarta tranche coperta da unit
  test Python web/model-only; quinta tranche coperta da unit test SQLAlchemy
  model-only; sesta tranche coperta da unit test Prisma-only e regressione
  Node/TS; settima tranche coperta da unit test SQL raw e regressioni
  Prisma/Python fallback; ottava tranche coperta da unit test Drizzle e
  regressioni Node/TS/Prisma; nona tranche coperta da unit test Doctrine e
  regressioni Symfony/Laravel.

### P2-3 Performance, Cost And Storage Controls

- Area: Backend, sync, agent.
- Objective: Tenere diagnosis veloce e sostenibile.
- Status 2026-07-07: quinta tranche locale completata. Artifact upload ora usa
  una cache content-addressed per workspace binding, schema e HEAD commit:
  artifact identici vengono saltati con `artifacts_skipped`, lo status continua
  a considerarli copertura valida, e la CLI mostra il totale artifact
  uploaded+skipped. Lo sync summary include anche `duration_ms`, visibile in
  status/dashboard, per baseline di costo temporale. La terza tranche registra
  anche manifest e delta per-file degli artifact in cache sync, con conteggi
  added/changed/removed/unchanged. La quarta tranche comprime i payload artifact
  grandi con `gzip+base64` quando conviene, invia dimensioni/hash del JSON
  originale, mantiene fallback raw se un backend legacy rifiuta il formato, e
  il backend Hades decodifica/verifica/stora lo stesso JSON artifact canonico.
  La quinta tranche aggiunge `hades backend benchmark`, un benchmark sintetico
  source-free per artifact medi/grandi che misura bytes raw/compressi,
  compression ratio, hash e durata locale.
- Evidence: Artifact JSON e source slices possono crescere; live recall ha gia'
  timeout aggressivo. Prima di questa tranche job ripetuti potevano ricaricare
  lo stesso graph/tree artifact.
- Affected paths: sync jobs, backend indexes, provider timeouts, retention.
- What/How: incremental indexing, chunk budgets, compression, cache etag,
  per-domain limits, background reindex scheduling. Prima tranche locale:
  dedup upload artifact; seconda tranche: durata sync; terza tranche:
  manifest/delta file-level locale; quarta tranche: compressione artifact
  local+backend; quinta tranche: benchmark sintetico medio/grande; restano
  delta-upload backend effettivo e benchmark su dataset reale.
- Dependencies: P0/P1 schemas.
- Acceptance criteria: sync incrementale, search sotto budget, storage bounded.
  Prima tranche evita upload duplicati per artifact invariati; seconda tranche
  registra la durata sync per benchmark operativo; terza tranche rende
  osservabile il delta file-level per artifact; quarta tranche riduce i payload
  di artifact grandi senza cambiare il JSON archiviato lato backend; quinta
  tranche rende ripetibile un guardrail locale su payload medi/grandi.
- Verification: benchmark dataset medio/grande. Prima tranche coperta da unit
  test su upload skip e awareness status; seconda tranche coperta da smoke sync
  e test runner; terza tranche coperta da unit test su manifest/delta per-file;
  quarta tranche coperta da unit test locali su compressione/fallback raw e
  feature test backend su artifact compresso; quinta tranche coperta da unit
  test benchmark e comando JSON.

### P2-4 Documentation, Runbooks And Support

- Area: Docs, support, release gates.
- Objective: Rendere operabile il sistema da utenti e maintainer.
- Status 2026-07-07: prima tranche completata. `hades backend support-report`
  produce un report locale redatto con stato setup/sync/awareness/action senza
  path assoluti, raw source o token; support docs lo indicano come evidenza
  preferita rispetto a `status --json` raw.
- Evidence: Esistono Hades operations/support docs, ma mancava un dry-run
  support scenario token-free e path-safe.
- Affected paths: `docs/hades/*`, `docs/RELEASE_GATES.md`, support runbook.
- What/How: guida no-codebase diagnosis, privacy, stale recovery, bug intake,
  evidence export, failed-index troubleshooting. Prima tranche: support report
  redatto e runbook aggiornati; resta una guida diagnosis end-to-end piu'
  ampia.
- Dependencies: P0/P1.
- Acceptance criteria: support puo' diagnosticare setup senza raw secrets/source.
  Prima tranche copre setup/sync/awareness/actions senza path assoluti.
- Verification: docs audit e dry-run support scenario. Prima tranche coperta da
  unit test CLI sul report redatto.

### P2-5 Governance And Continuous Quality Review

- Area: Product, QA, security.
- Objective: Impedire regressioni di awareness, privacy e causal quality.
- Status 2026-07-07: quarta tranche locale/dashboard completata. `hades backend
  quality-report` aggrega la suite no-codebase e il support report locale in
  `hades.quality_report.v1`, con action queue e severita' `blocker` per
  regressioni causal/privacy/awareness e `warning` per setup locale incompleto.
  `--record` salva l'ultimo report nello status locale e la dashboard backend
  espone status, blocker, warning e action queue corrente. La quarta tranche
  aggiunge `generated_at` e staleness nello status locale: report mancante o
  piu' vecchio di 7 giorni produce action di governance, senza marcare il
  backend degraded se l'ultimo report era passed.
- Evidence: Un sistema di memoria condivisa peggiora se facts, chunks e stale
  claims non sono revisionati. Prima di questa tranche esisteva l'evaluator
  no-codebase, ma non un report governance con action queue.
- Affected paths: release gates, dashboard review queues, tests.
- What/How: review periodica di low-confidence diagnosis, stale facts,
  source-slice access, false positives/false negatives della suite. Prima
  tranche: quality report CLI; seconda/terza tranche: snapshot registrabile e
  dashboard metrics; quarta tranche: stale/missing report actions; resta
  scheduler periodico vero.
- Dependencies: P1 observability.
- Acceptance criteria: quality report periodico e action queue. Prima tranche
  produce report/action queue manuale e CI-friendly; terza tranche registra e
  mostra l'ultimo report in dashboard; quarta tranche segnala baseline mancante
  o stale. Resta scheduler audit periodico.
- Verification: scheduled audit job e dashboard metrics. Terza tranche coperta
  da unit test quality report/status web e typecheck dashboard.

## Area Plans

Backend:

- Deve diventare il sistema autorevole per bug evidence, code graph, source
  slices, freshness, ACL e diagnosis reports.
- Le API nuove vanno nel namespace Hades v1 e devono restare scoped per project,
  workspace binding e agent token.
- La memoria generica non deve contenere raw source o log grezzi: deve contenere
  facts verificati, resolved bugs e summary citabili.

Agent:

- Deve usare memoria condivisa ad ogni turno come gia' fa, ma per bug diagnosis
  deve passare da "recall utile" a "workflow causale".
- I nuovi tool devono essere service-gated e disponibili solo quando backend,
  project binding e capability sono presenti.
- L'agent deve saper dire no: se freshness/evidence mancano, la risposta deve
  chiedere l'evidenza specifica mancante.

Frontend/Desktop:

- Deve mostrare lo stato di readiness, non solo "backend warning".
- L'utente deve poter vedere: progetto linked, commit indicizzato, graph/source
  coverage, bug cases, evidence pack, policy source e stale reasons.
- Le approval per leggere/salvare source slices devono essere esplicite.

Data Quality:

- Le note devono diventare facts verificati o restare note non verificate.
- I raw chunks devono restare quarantined o evidence, mai recall automatico.
- Ogni root cause salvata deve avere evidence refs, confidence e data/commit.

Operations:

- Release gate dedicato: no-codebase diagnosis suite.
- Doctor/status devono esporre coverage e stale state.
- Logs/metrics devono essere redatti e non contenere source/log payload raw.

## Risks And Decisions

Rischi principali:

- Falsa precisione: l'agent potrebbe parlare con sicurezza quando ha solo
  metadata. Mitigazione: P0-1 e P0-3 come gate hard.
- Privacy/source leakage: source slices e logs possono contenere segreti.
  Mitigazione: P0-8 prima di qualunque store content-bearing.
- Search rumorosa: LIKE/artifact JSON non basta per causal debugging.
  Mitigazione: P1-3 search strutturata e graph traversal.
- Staleness invisibile: un indice vecchio puo' produrre cause sbagliate.
  Mitigazione: freshness coverage in ogni response e UI.
- Coupling core: aggiungere tools core romperebbe la filosofia Hermes.
  Mitigazione: provider tools service-gated e backend APIs.
- Over-indexing costoso: indicizzare tutto puo' essere lento/caro.
  Mitigazione: source slices selected, incremental indexing, retention.

Decisioni da prendere prima di implementare source slices:

- policy default per progetti privati: off, selected-on-approval, o on;
- retention default di source slices e runtime logs;
- chi puo' approvare accesso source in team;
- quali bug evidence sono condivisibili tra device;
- quali framework vanno considerati P0 dopo Laravel.

## Final Note

La strada piu' breve verso l'obiettivo non e' aggiungere un altro sistema di
memoria. E' trasformare Hades backend in un evidence system: memoria condivisa,
graph aggiornato, source slices policy-compliant, bug evidence e freshness
devono lavorare insieme.

Quando questi P0 saranno completati, l'agent potra' spiegare una causa precisa
senza codebase live nei casi in cui il backend contiene evidence corrente e
copertura sufficiente. Nei casi mancanti, dovra' rispondere con precisione
diversa: non "non so", ma "mi manca X per distinguere causa A da causa B".
