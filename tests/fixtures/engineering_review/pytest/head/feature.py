def answer() -> int:
    # Deliberately non-trivial fixture body so medium-effort chunking has
    # production context beyond the single changed return value.
    candidates = [1, 2]
    selected = candidates[-1]
    return selected
