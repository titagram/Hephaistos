# Task 3 Fix Round 1 Report

## Scope

- Gave the Cytoscape graph canvas a plugin-owned responsive minimum height so
  it remains functional before later host CSS is available.
- Replaced the truncation notice's false promise with an explicit, accessible
  **Expand selected neighborhood** action. It fetches the selected root with
  `depth=2`, `limit=200`, the current supported API filters/search, and the
  immutable expected revision.
- Resetting filters now clears selection and the graph root, returning to the
  unrooted bounded graph. Fetch failures retain the currently displayed graph
  and show the existing last-valid-graph status notice.

## TDD evidence

- The canvas sizing test was added first and failed because the graph canvas
  had no inline sizing contract (`style` was undefined).
- The neighborhood interaction test was added first and failed because no
  expansion action was rendered after selection.

## Verification

- `npm run test --workspace web -- --run src/plugins/evolution-plugin.test.ts src/plugins/evolution-api.test.ts src/plugins/evolution-graph.test.ts src/plugins/evolution-organism-view.test.ts` — 18 passed.
- `npm run check:evolution --workspace web` — TypeScript check, bundle rebuild,
  and bundle syntax validation passed.

## Note

`dist/index.js` was regenerated by the prescribed bundle check. It retains the
pre-existing reviewer whitespace changes in vendored bundle comments; these
were not discarded.
